# I need to implemet this
def add(x,y):
  # if x and y are both integers then add them
  if (type(x) == int and type(y) == int):
    return x+y
  # Otherwise retuen None
  return None
  
#Bob needs to implemet this
def mult(x,y):
  return x*y
